/**
 * Layout for bracket page
 * /app/league/[leagueId]/bracket/layout.tsx
 */

export default function BracketLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
